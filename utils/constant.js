export const regex = {
    phoneNumber: /^[0-9]{10}$/,
    otp: /^[0-9]{6}$/
}

// User Related constants
export const minNameLength = 2;
export const maxNameLength = 50;

// DB connection
export const dbConnectionRetries = 5;